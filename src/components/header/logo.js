import React from 'react';

const Logo = (props) => {
    return (
        <div className={' text-2xl fixed flex items-center justify'} >
            Timee
        </div>
    );
}

export default Logo;